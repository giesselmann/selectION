# travis.yml for https://github.com/giesselmann/selectION

language: cpp

before_script:
    - sudo apt-get -qq update
    - sudo apt-get install -y libboost-filesystem1.46-dev libboost-program-options1.46-dev libboost-system1.46-dev
    
script: 
    - mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Release .. && make
    - src/selection